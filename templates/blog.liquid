<div data-barba="wrapper">
  <div id="blog" data-barba="container" data-barba-namespace="blog">
    <div id="blog-content">
      <div id="blog-hero">
        <div class="blog-hero-content">
          <div class="blog-hero-left">
            <img
              src="https://cdn.shopify.com/s/files/1/0367/2417/9081/files/Mask_Group_28_2x_0fa63a01-26d0-4b3a-895c-a5ad1c2f8393.png?v=1586484248"
              alt="">
          </div>
          <div class="blog-hero-right">
            <h2>
              Lazo Libro un nueva propuesta editorial.
            </h2>
            <p>Vivimos obsesionados. Investigamos y transformamos información y datos en narraciones y libros
              ilustrados. Le damos un lugar a la información escondida y acompañamos a los lectores en su descubrimiento
              del mundo.</p>
              <div class="blog-post-link">
                <a href="">LEER MÁS</a>
              </div>

          </div>

          <div class="hero-link">
            <img src="https://cdn.shopify.com/s/files/1/0367/2417/9081/files/hero-arrow.svg?v=1585454452" alt="">
            <span>SCROLL</span>
          </div>

        </div>
      </div>

      <div id="blog-content">
        <div id="blog-bg">
          <div class="blog-entries">

            {% for article in blog.articles %}
            <div class="blog-singular">
              <div class="singular-image">
                <a href="{{ article.url }}">
                  <img src="{{ article.image.src | img_url: 'grande' }}">
                </a>

                <span>{{article.tags}}</span>
              </div>
              <h2><a href="{{ article.url }}">{{ article.title }}</a></h2>
              {% if article.excerpt.size > 0 %}
              <p>
                {{ article.excerpt }}
              </p>
              {% else %}
              <p>
                {{ article.content | strip_html | truncatewords: 100 }}
              </p>
              {% endif %}
               
                
              
              <div class="blog-singular-link">
                <a href="{{ article.url }}">LEER MÁS</a>
              </div>
            </div>
            {% endfor %}
      

          </div>
        </div>
      </div>


      {% paginate blog.articles by 5 %}

    
     
      {% if paginate.pages > 1 %}
      {{ paginate | default_pagination }}
      {% endif %}

      {% endpaginate %}




    </div>
  </div>